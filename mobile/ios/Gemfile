source "https://rubygems.org"

# Local and CI both use Ruby 3.3 (see .ruby-version) — the macOS system
# Ruby 2.6 cannot resolve this Gemfile (CFPropertyList >= 4 needs 3.2+).
ruby "~> 3.3.0"

gem "fastlane", "~> 2.228"
