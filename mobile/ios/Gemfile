source "https://rubygems.org"

gem "fastlane", "~> 2.228"

# Locked explicitly: 3.0.9 requires ruby < 3.2 and 4.0.0 requires >= 3.2, so a
# lockfile resolved on one Ruby breaks bundle install on the other; 3.0.8 is
# the newest version installable everywhere.
gem "CFPropertyList", "3.0.8"
